server:
  port: 8081

spring:
  security:
    oauth2:
      client: # ????? ??
        registration:
          keycloak:
            client-id: oauth2-client-app  # ??? ???? ??? ????? ???
            client-name: oauth2-client-app  # ????? ??
            client-secret: BEcxjQZth2VU0A9llS8hwE3qJXvNMLXR  # ??? ???? ??? ????? ????
            redirect-uri: http://localhost:8081/login/oauth2/code/keycloak # ???? ??? ?????? ??????? ?? - ????????/login/oauth2/code?? ??????.
            authorization-grant-type: openid # ?? ?? ??
            client-authentication-method: client_secret_basic # ????? ???? ?? ?? - client_secret_basic? base64 ??? ? ??? ??? ?? ? ?? post, none
            scope: # ??? ?? ??
              - openid
              - profile
              - email
        # http://localhost:8080/realms/oauth2/.well-known/openid-configuration ???? ??
        provider: # ??? ??
          keycloak:
            authorization-uri: http://localhost:8080/realms/oauth2/protocol/openid-connect/auth # ?? ?? ?????
            token-uri: http://localhost:8080/realms/oauth2/protocol/openid-connect/token # ??? ??? ??
            issuer-uri: http://localhost:8080/realms/oauth2 # OAuth2.0 JwkSetUri ?????
            user-info-uri: http://localhost:8080/realms/oauth2/protocol/openid-connect/userinfo # OAuth2.0 ?? ?? ???
            jwk-set-uri: http://localhost:8080/realms/oauth2/protocol/openid-connect/certs # OAuth2.0 UserInfo ?? ???
            user-name-attribute: preferred_username # ????? ???? ????